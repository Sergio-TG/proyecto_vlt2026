import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Recuperar contraseña | Viví las Termas",
  robots: { index: false, follow: false },
}

export default function RecuperarClaveLayout({ children }: { children: React.ReactNode }) {
  return children
}
