import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Actualizar contraseña | Viví las Termas",
  robots: { index: false, follow: false },
}

export default function ActualizarClaveLayout({ children }: { children: React.ReactNode }) {
  return children
}
